import type{MetadataRoute}from"next";import{pages}from"@/data/site";export default function sitemap():MetadataRoute.Sitemap{const base="https://hajjumrahtours.com";return[{url:base,lastModified:new Date(),changeFrequency:"weekly",priority:1},...pages.map(p=>({url:`${base}/${p.slug}`,lastModified:new Date(),changeFrequency:"monthly" as const,priority:p.category==="Umrah"?.9:.7}))]}
